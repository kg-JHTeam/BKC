<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SideMenuDAO">

	<!-- 전체 사이드메뉴 조회 -->
	<select id="getSidemenuList" resultType="SideMenuVO">
		<![CDATA[
		select img_path, product_name from product P inner join product_image I on P.product_serial = I.product_serial;
		]]>
	</select>
	<!-- 사이드메뉴 디테일 -->
	<select id="getSidemenu" resultType="SidemenuVO">
		select img_path, product_name, description from product P inner join product_image I on P.product_serial = I.product_serial;
	</select>

</mapper>