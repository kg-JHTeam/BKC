<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/security
        https://www.springframework.org/schema/security/spring-security.xsd">
	
	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user authorities="ROLE_ADMIN" name="admin"  password="{noop}admin" />
				<user authorities="ROLE_USER" name="manager" password="{noop}manager" />
			</user-service>
		</authentication-provider>
		
		<!-- DB 설계후 DB에 관리자 추가 -->
		<!--  <authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				authorities-by-username-query="select id, authority from authories where id =?"
				users-by-username-query="select id, password, enable from users where id =?" />
		</authentication-provider>-->
	</authentication-manager>

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/" access="permitAll" />
		<intercept-url pattern="/*.mdo" access="hasRole('ROLE_ADMIN')" />	<!-- 관리자만 들어갈 수 있음. -->
		<intercept-url pattern="/*.do" access="permitAll"/>
		<intercept-url pattern="/resources/**" access="permitAll" />
		
		<!-- 관리자 로그인 페이지  -->
		<form-login login-page="/adminLogin"  authentication-failure-url="/adminLogin?error" />
	</http>
</beans:beans>
